<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="title"
            type="String" />

        <variable
            name="message"
            type="String" />

        <variable
            name="yesHandler"
            type="android.view.View.OnClickListener" />

        <variable
            name="noHandler"
            type="android.view.View.OnClickListener" />

    </data>

    <LinearLayout
        android:id="@+id/linear_layout_dialogyesno"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/txt_title_dialogyesno"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@color/colorPrimary"
            android:gravity="center"
            android:text="@{ title }"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            tools:background="@color/colorPrimary"
            tools:text="Lorem Ipsum" />

        <TextView
            android:id="@+id/txt_message_dialogyesno"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="6dp"
            android:gravity="center"
            android:text="@{ message }"
            android:textSize="18sp"
            tools:text="Lorem ipsum sit dolor amet..." />

        <LinearLayout
            android:id="@+id/layout_button_panel_dialogyesno"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_yes_dialogyesno"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_margin="4dp"
                android:layout_weight="1"
                android:backgroundTint="@color/colorSuccess"
                android:onClick="@{ yesHandler::onClick }"
                android:text="@android:string/yes"
                android:textAlignment="center"
                android:textColor="@android:color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_no_dialogyesno"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_margin="4dp"
                android:layout_weight="1"
                android:backgroundTint="@color/colorError"
                android:onClick="@{ noHandler::onClick }"
                android:text="@android:string/no"
                android:textAlignment="center"
                android:textColor="@android:color/white" />

        </LinearLayout>

    </LinearLayout>
</layout>